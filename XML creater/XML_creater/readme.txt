Hi, this program is written by @Ruolin Huang using Java.

It can convert automatically *.txt file into *.xml file. There are test.txt shown as an example. This program can be helpful for converting a PDF file into XML file, if we can scan the information from PDF as String type.

Java version: 12.0.1